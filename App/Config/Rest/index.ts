// the url other options (like auth, ect ...) to call the API
export const REST = {
    // can be replace by https://mysafeinfo.com/api/data?list=alcoholicbeverages&format=json
    baseUrl: '/App/Mock/alcoholicbeverages.json',
};
